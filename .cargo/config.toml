[build]
target = "x86_64-unknown-none"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"


[target.x86_64-unknown-none]
rustflags = [
    "-C", "relocation-model=static",
    "-C", "link-arg=-Tboot/linker.ld",
]